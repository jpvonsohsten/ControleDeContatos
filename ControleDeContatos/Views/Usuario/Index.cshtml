@model List<UsuarioModel>

@{
    ViewData["Title"] = "Lista de Usuários";
}

@if(TempData["MensagemSucesso"] != null)
{
    <div class="alert alert-success text-center" role="alert">
        <button type="button" class="btn btn-sm close-alert" arial-label="Close">X</button>
        @TempData["MensagemSucesso"]
    </div>
}

@if(TempData["MensagemErro"] != null)
{
    <div class="alert alert-danger text-center" role="alert">
        <button type="button" class="btn btn-sm close-alert" arial-label="Close">X</button>
        @TempData["MensagemErro"]
    </div>
}

    
<h1 class="display-4 text-center">Lista de Usuários</h1>

<div class="d-grid gap-2 d-md-flex justify-content-md-start mt-5 mb-5">
    <a class="btn btn-success text-white rounded-bottom" role="button" asp-controller="Usuario" asp-action="Criar">Adicionar Usuário</a>
</div>
 
<table class="table mt-5" id="table-usuarios">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Nome</th>
            <th scope="col">Login</th>
            <th scope="col">Perfil</th>
            <th scope="col">Email</th>
            <th scope="col">Data de cadastro</th>
            <th scope="col"></th>
        </tr>
    </thead>
    <tbody>      
        @if(Model != null && Model.Any())
        {
            foreach (var usuario in Model)
            {
                <tr>
                    <th scope="row">@usuario.Id</th>
                    <td>@usuario.Nome</td>
                    <td>@usuario.Login</td>
                    <td>@usuario.Perfil</td>
                    <td>@usuario.Email</td>
                    <td>@usuario.DataCadastro</td>
                    <td>
                        <div class="btn-group" role="group">
                            <a role="button" class="btn btn-dark text-white" asp-route-id="@usuario.Id" asp-controller="Usuario" asp-action="Editar">Editar</a>
                            <a role="button" class="btn btn-danger text-white" asp-route-id="@usuario.Id" asp-controller="Usuario" asp-action="ExcluirConfirmacao">Excluir</a>
                        </div>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>

@* @(usuario.Perfil == ControleDeContatos.Enums.PerfilEnum.Admin ? "Administrador" : "Padrão") *@